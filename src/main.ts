// Дана строка, необходимо создать массив и записать в него каждый символ строки
const lines = 'Goga!!!';
const arrayText = [];
for (const line of lines) {
  arrayText.push(line);
}
console.log(arrayText);
